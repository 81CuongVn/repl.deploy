interface:
  display_name: "Repl.deploy"
  short_description: "Repo-specific patterns and workflows for repl.deploy"
  default_prompt: "Use the repl.deploy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true